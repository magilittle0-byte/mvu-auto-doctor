# MVU 自动医生（通用）

这是一个独立的 SillyTavern / TauriTavern 扩展。它包含通用 MVU 审计修复、可选的平行支线连续性账本和不占正文的内置世界论坛，不会修改角色卡、SP·数据库、预设、缝合怪、Zsd 或 Story Oracle 文件。

## 安装

在 TauriTavern 的扩展安装页面选择“从 Git 仓库安装”，粘贴：

```text
https://github.com/magilittle0-byte/mvu-auto-doctor
```

安装后刷新一次页面。后续版本可直接使用扩展管理里的“更新”功能，不需要重新下载压缩包。

每次收到新的 AI 回复后，扩展会：

1. 动态读取当前角色卡暴露的 MVU/Zod Schema。
2. 动态读取当前启用世界书中的 `[mvu_update]` 规则。
3. 等待本轮普通 MVU 更新稳定，避免在变量仍写入时抢跑。
4. 在开局窗口内检查“初始化满值、派生上限已提高、当前值却冻结”的资源失配，并用本地规则安全同步，不调用额外模型。
5. 对比上一 AI 楼层状态、当前 `stat_data`、本轮正文和原更新区块。
6. 让模型只生成针对当前状态的纠错/补漏补丁。
7. 在内存副本上校验路径、操作、MVU 解析和 Zod 结果。
8. 全部通过后才原子写入，并在写入后回读验证。
9. 把纠错块持久附加在当前 swipe 中；刷新或重进聊天后，原错误块与纠错块会按顺序重放，不会把旧错误复活。
10. 刷新消息与 `<StatusPlaceHolderImpl/>`，让正文状态栏立即重建。

v1.2.0 开始，每次任务还会绑定聊天 ID、稳定消息 ID、楼层、swipe、正文指纹和生成世代，并在每个异步步骤后复核。数据库预热/算 Token 的 `dryRun` 会被忽略；普通生成、swipe、regenerate 与 continue 分开记录。真正写变量时使用精确楼层而不是笼统的 `latest`，最大限度避免切聊天、重抽或连续生成时落错位置。

模型连接顺序：

- 默认优先使用 Story Oracle 已配置的连接（稳定 Hook API v1）。
- Story Oracle 未安装、未配置或调用失败时，自动使用酒馆当前主连接的 `generateRaw`。

为避免两个自动程序同时写变量，扩展默认会关闭 Story Oracle 的 `AUTO` 诊断开关；Story Oracle 的手动诊断不受影响。

## 必须这样设置

- 关闭 MVU 的“额外 AI 解析/额外模型更新变量”。本扩展跟随角色卡原本的 `<UpdateVariable>` 更新完成后再审计，不需要第二个 MVU 解析模型。
- 只保留一个自动修复器：开启“MVU 自动医生”，关闭 Story Oracle 的 `AUTO` 诊断。Story Oracle 的手动诊断、问剧情、校正回复、世界书模式仍可正常使用。
- SP·数据库继续使用作者原版填表提示词。自动医生不读写数据库表，也不接管数据库回忆。
- 同一角色只启用一套当前版本的 MVU 更新规则。若作者卡已内嵌世界书，不要再同时启用一套重复旧世界书。

“关闭 MVU 额外 AI 解析”只针对 MVU 自带的第二次变量解析。支线连续性开启时，会在主回复落地后使用插件连接整理一份独立的因果账本，但它不写 MVU，也不代替角色卡原本的变量更新。

## 开局资源初始化修正

设置中的“开局自动补满初始化失配的资源”默认开启。它用于修复这类角色卡错误：世界书把资源初始化为满值（例如 MP 50/50），角色创建完成后前端把上限派生为 110，却只留下当前值 50。

- 只在当前聊天最前面的 4 条 AI 消息内运行，包括角色卡开场白。
- 优先读取当前卡或绑定世界书的 `[initvar]`/“变量初始化”条目；没有可读初始化条目时，才用上一楼确实满值的状态作证据。
- 不写最大值，只把已证明属于初始化失配的“当前值”同步到现有最大值。
- 不依赖 HP、MP 等专属字段名，会识别中英文“当前/最大/上限”成对字段，因此可用于其他 MVU 卡。
- 初始化不是满值的容量字段不会动，例如负重 0/55；本轮更新区块明确改过的资源也不会动，因此不会把战斗消耗或主动施法误判成开局补满。
- 多阶段捏人再次提高上限时，只要该资源仍保持上一步同步后的满值，可以在开局窗口内继续同步。
- 每次修正同样经过 MVU 解析、Schema、精确楼层写入、写后回读和可撤销记录，不是仅修改前端显示。

## 平行支线连续性

该功能不会强行在正文末尾公开所有幕后事件，而是把状态保存在当前聊天的插件元数据中，并在下一回合以短账本注入。v1.4.0 起，每个完成的 AI 回复都会运行一次世界节拍；“平行事件”不是正文衍生支线的同义词，而是四类并存的活世界事件：

- `主线衍生`：由正文中已经发生的行动、人物或因果直接产生。
- `世界设定·暗中相关`：尚未在主线露面，但按世界设定存在潜在因果，等待真实传播链或接触条件。
- `世界设定·当前独立`：依据角色卡/当前世界书独立发展，可能很久以后自然相交，也可以永远不相交。
- `世界脉动`：组织、社会、生态、日常生活或局势在玩家视野外自行变化，可以在幕后自行结束。

运行规则：

- 识别预设的 `<parallel_event_record>`、`<current_event>`，以及缝合怪的 `<dm_story>`、`<npc_track>`。
- 给事件保留稳定 ID、来源类型、因果父项、与主线关系、阶段、幕后变化、触发条件、下一拍、自然汇流条件、知情层级、持续影响、流言与设定依据。
- 每个完成的 AI 回合都会运行一次调度。通常让一条未结事件推进、显现、休眠或结束；若正文只过去片刻、事件时间窗未到或因果前提未满足，可以登记一次带有具体事件与事实依据的 `held`。空泛的“无变化”不算有效调度，会自动重试一次。
- 玩家本轮 A 行动直接造成持续 B 影响时，可以在推进旧事件的同一回合登记 main_derivative 事件 C；不再发生“推进旧事件就丢掉新因果”的二选一。
- 事件结束时必须登记结束方式和至少一项持续影响或流言。仍会自行发展的 D 后果使用 `causedBy` 派生新稳定事件；旧事件保持 resolved，避免把同一支线无限续命。
- 每轮最多推进一条旧事件、最多接纳两条有正文或父事件证据的因果新事件；无直接因果的世界脉动仍遵守自主度节流，避免事件爆炸。
- hidden / rumor / observed 分层，禁止让不知情角色突然全知。
- 只推动 NPC、势力、敌方、环境、约定与离场角色；禁止替玩家角色行动、回答、移动、消费资源或追加检定。
- 检测到预设或缝合怪时采用桥接模式，由原系统保留剧情提案权；插件只做接续、去重和回收。
- 缝合怪的成功/失败路线保留为条件式导演提案，不作为裁决目标。先按当前骰子前端的固定位置或顺序消费唯一骰值并结算 DC/成功等级，再选择对应剧情分支；禁止浏览骰池挑成功数字或先写结果后补造检定。
- 检测到 DlSNlGHT/World 世界引擎时同样进入桥接模式：世界引擎保留事件链、风声与世界推演提案权，变量医生不再把自己当唯一导演，只补缺失因果并避免同一事件重复推进。
- 自主事件只从当前角色卡与当前启用世界书的叙事设定取材，可创建符合设定的普通 NPC、小组织和地方事务，但不能凭空改写核心世界观或重要角色过去。
- 独立事件默认只在幕后账本推进，不为了展示伏笔强塞进正文；只有传播、因果、人物接触、地点或时间条件真实满足时，才先进入“正在汇流”。
- 独立事件允许一直不与主线相交，也允许在幕后解决。结束后的影响和有传播链的流言仍可继续改变世界，插件不会把所有变化都变成围绕玩家的任务。
- 已结束事件进入独立归档容量，不再占用活动事件槽位；长期游玩不会因为历史事件塞满上限而停止生成。

模式说明：

- `自动活世界（推荐）`：有角色卡/世界书叙事设定时即可在回复后维护活世界；检测到预设/缝合怪时自动桥接去重。
- `始终开启`：没有结构化标记时也整理正文中的明确未决因果。
- `关闭`：撤下注入，不运行支线整理；已有账本仍保留，除非手动清空。

自主度说明：

- `保守`：只登记正文、预设或缝合怪已经提出的因果，不主动建立幕后事件。
- `活世界（推荐）`：自主新事件至少间隔 3 个账本轮次，最多保留 3 条未结自主事件。
- `活跃`：自主新事件至少间隔 2 个账本轮次，最多保留 4 条未结自主事件。

重抽或重新生成时，正文生成阶段使用该楼回复产生前的支线存档点；新 swipe 落地后再从存档点重算，不让被放弃的旧 swipe 污染新剧情。

## 世界论坛

v1.4.1 起，“打开独立论坛”可以直接打开扩展自带的论坛，不再要求安装 Zsd。论坛以当前聊天为单位保存帖子、评论、版块、热度与刷新页次，并使用独立模型请求增量刷帖，不占正文篇幅、不写 MVU、数据库或支线账本。v1.4.2 起可在设置和完整论坛顶部明确选择“医生内置论坛”或“Zsd 论坛”，来源、自动刷新开关和刷新间隔会直接显示，不再根据是否检测到 Zsd 静默停更。

- 首次刷新生成 4 至 5 个帖子和 6 至 12 条评论，每个首屏帖子都至少有一条回复；后续每次生成 2 至 4 个新帖和 6 至 12 条评论，并优先给零回复旧帖补楼。
- 至少一半内容是日常闲聊、求助、攻略、交易、吐槽、八卦或地方话题；不同网友会补充、质疑、开玩笑或跑题，不把整个论坛变成任务板。
- 生成结果若帖子或评论数量不足、首屏仍有孤帖、后续没有补零回复旧帖，会自动拒绝并重试一次，不把不合格的一页保存到聊天。
- 只把 observed / rumor 等已经具备公开传播条件的世界风声交给论坛；hidden 幕后事件和私密正文不会泄露给网友。
- 普通帖子永远留在论坛。只有帖子已经造成持续的聚集、传播、短缺、人物决定或外部行动时，下一轮支线调度才可把它登记为因果事件；网友猜测不能倒推成真相。
- 支线产生公开风声后可以进入论坛；论坛形成真实公共行动后又可在下一轮回到支线账本，再通过连续性注入自然影响正文，形成闭环而不是三套系统各写各的。
- 默认每个 AI 回合刷新一次，可在设置中调整为每 1 至 12 回合一次，也可关闭自动刷新后只手动刷帖。
- 打开一个尚无帖子的内置论坛时会自动刷新第一页；之后仅打开、关闭、切换分类或展开评论不会调用模型。
- 重抽会使用该楼刷新前的论坛存档点，旧 swipe 生成的帖子不会污染新回复；切换聊天后只显示新聊天自己的论坛。
- 默认来源为“医生内置论坛”，即使安装了 Zsd 也会继续按回合刷新，解决安装 Zsd 后医生论坛看似失效的问题。
- 主动把来源切换为“Zsd 论坛”后，医生才暂停自己的内置自动刷新，并把悬浮入口改为打开 Zsd。两边帖子数据互不覆盖；如果选择内置来源但仍开启 Zsd 自己的自动生成，两套插件会产生各自的模型请求，页面会明确提醒。

### 手机端查看支线账本

安装后默认出现一个写有“脉”字的悬浮球，也仍可在“扩展设置 → MVU 自动医生（通用）→ 平行支线连续性”查看同一份账本：

- 悬浮球可上下拖动，松手后吸附到最近的左右边缘，闲置时只露出一小段；点击即可拉出并打开面板。位置会保存在本机。
- 角标显示当前未结事件数；整理中、成功或失败会使用不同的光效提示。可在扩展设置关闭悬浮球，状态栏入口始终保留。
- 悬浮面板分为“支线 / 风声 / 论坛 / 工具”四个页签，各自显示数量角标并记住上次所在页；支线、留言风声和操作按钮不再堆成一张需要长距离翻动的页面。
- 面板提供“检查变量”“整理世界”和论坛入口。论坛入口会遵循你选择的来源；选择内置论坛时进入移动端完整论坛，若当前聊天还没有帖子会自动生成第一页，但不会占用主回复。
- 如果检测到 Zsd，内置论坛会显示兼容提醒和“打开 Zsd”快捷按钮；是否暂停内置自动刷新只由可见的“论坛来源”选项决定。

- 顶部显示未结数、已收束数、账本轮次、更新时间和当前来源模式。
- 未结支线按“已显现 → 推进中 → 已埋设 → 搁置”及紧迫度排序；第一条不含剧透的事件默认展开。
- 尚未在主线显现的独立事件默认只显示“幕后独立事件（点击查看剧透）”，避免玩家提前失去惊喜；可关闭剧透折叠。
- 点击每条支线可查看事件来源、因果父项、与主线关系、设定依据、当前进展、幕后变化、结束方式、持续影响、传播中的流言、下一自然接口、触发条件、汇流条件、人物地点、知情范围和最近来源楼层。
- 已收束支线放在单独的折叠区，不与活动支线混在一起。
- “世界风声”单独汇总事件产生且已经形成传播链的消息。普通水帖、吐槽、玩笑和日常互动不是支线，继续由卡内贴吧或独立论坛生成。
- “刷新显示”只重读当前聊天元数据，不调用模型；“立即整理支线”才会调用模型更新账本。
- 该区域是玩家审计视图；只有你主动展开剧透卡片时才显示角色尚不知情的幕后信息，插件不会因此把隐藏知识泄露给正文角色。

切换聊天、收到新回复、整理完成、重抽或清空账本后，显示会自动刷新。选择“关闭”只停止后续整理，旧账本仍可查看。

扩展设置里提供：

- 自动开关
- 开局初始化资源满值同步开关
- 模型连接优先级
- 双写保护
- 回复后等待时间
- 手动检查最新回复
- 跨刷新保存、并经过变量指纹保护的撤销记录
- 平行支线的自动兼容/始终开启/关闭模式
- 活世界自主度与幕后事件剧透折叠
- 手动整理支线与清空当前聊天账本
- 手机端可展开的支线账本明细与已收束归档
- 可拖动、贴边半隐藏的悬浮球，以及不占正文的完整内置世界论坛
- 论坛来源选择、内置论坛自动刷新频率、Zsd 双生成提醒、手动刷新和当前聊天清空

插件不包含任何角色卡专属字段、路径或枚举。角色卡更新或切换到其他 MVU 卡时，会重新读取新卡的规则和 Schema。

聊天元数据只保存有限条修复快照、支线账本、内置论坛帖子和来源指纹，不保存 API 密钥或完整模型提示词。单条 MVU 快照超过安全体积时不会写入聊天，此时该条修复仍然有效，但无法跨刷新撤销。

## 版本记录

- v1.4.2：修复安装 Zsd 后内置论坛被静默暂停、首屏只有孤帖且无回复的问题；新增可见的内置/Zsd 来源切换和自动刷新状态；首屏强制 4 至 5 帖、每帖有回复并合计 6 至 12 条评论，后续每轮新增 2 至 4 帖、6 至 12 条评论并优先补零回复旧帖；不合格批次会自动拒绝重试；新增多回合自动刷新、孤帖修补和双来源切换回归。
- v1.4.1：把原先仅跳转 Zsd 的“独立论坛”补成真正的内置世界论坛；新增聊天隔离的帖子/评论/分类/热度页面、独立模型增量刷新、公开知识过滤、论坛与因果账本的双向闭环、重抽存档点、Zsd 自动暂停防冲突和手机全屏前端；悬浮面板改为支线/风声/论坛/工具四页；桥接模式新增“规划与裁决隔离”，保留缝合怪剧情推进能力，同时禁止规划反向影响骰池取值和成功等级。
- v1.4.0：修复活世界只在开局创建一次后停摆；每个 AI 回合运行一次有证据的调度，允许时间未推进时合理 `held`，允许推进旧事件时同时登记主线直接衍生因果，事件结束后保留影响/流言并派生后继事件，已结束事件不再占活动槽位；新增贴边悬浮球、世界风声分区、独立论坛快捷入口、无效调度重试与多回合浏览器回归。
- v1.3.1：新增通用开局资源初始化同步；读取 `[initvar]` 与前一楼满值证据，只修“派生上限提高但当前值冻结”的失配，并排除负重等非满容量、本轮消耗和开局窗口之外的状态。
- v1.3.0：把正文支线升级为四类活世界事件；读取当前角色卡与世界书叙事设定，允许独立幕后推进与自然结束；加入关系/汇流条件、硬性节流、自然汇流保护和手机端剧透折叠。
- v1.2.1：加入手机端支线账本视图，可直接查看活动/已收束支线、阶段、进展、下一拍、触发条件、人物地点、知情范围和来源楼层；切聊天与整理完成后自动刷新。
- v1.2.0：加入生成类型/dry-run 识别、任务失效世代、稳定消息身份、精确楼层写入、每个异步边界复核、跨刷新安全撤销，以及兼容预设和缝合怪的平行支线连续性账本。
- v1.1.0：纠错补丁持久写进当前 swipe；新增 MVU 稳定等待与同楼层去重。
