# MVU 自动医生 2.0 RC 使用说明

> 2026-07-28 RC 回归修订：TavernDB 等第三方脚本不需要注册医生的 barrier
> 协议。检测到数据库但没有合作 API 时，自检显示“可选协作/非托管”，不会标红或
> 阻止使用；外部写入一致性准确记为“未知”。医生自身的 failed/stale/late 结果仍
> 由完整目标身份和持久终态门拦截。

适用版本：`2.0.0-rc.1`

这是供候选测试使用的用户说明。`2.0.0-rc.1` 已发布到仓库默认分支 `main`，
酒馆可以直接在线更新；不需要加载分支列表，不需要导入 ZIP，也不需要删除扩展。

本次回归修复已经完成真实 MVU、数据库共存、同主模型 A/B、Scenario Plan 修订、
桌面与390×844触控、长局、重载和 stale 生命周期复验。宿主仍会报告
JS-Slash-Runner 自身缺失资源造成的404/MIME错误；医生、数据库和本次共存前端错误
均为0，报告没有宣称整个宿主控制台干净，也没有修改第三方。

## 1. 从现有1.x在线升级

1. 用酒馆自己的聊天导出/备份功能保存要测试的长聊天，并等待当前自动医生任务
   结束；聊天、角色卡、数据库和其他脚本保持原位。
2. 打开“扩展/已安装的扩展”，找到“MVU 自动医生（通用）”这一行。
3. 只点击这一行自己的“更新”按钮；不要点“全部更新”，也不要点击分叉/分支图标。
4. 更新完成后刷新酒馆，扩展信息应显示 `2.0.0-rc.1`。如果仍显示旧版，先保留
   当前页面和控制台信息，不要删除扩展或反复切换分支。
5. 打开自动医生设置，先运行环境自检，再检查严格变量通道和轻量通道所选的
   API 预设、模型与代理方式。两个通道可以共用一个连接，也可以分开配置。
6. 确保只有一个自动变量修复器负责写入。自动医生会尝试关闭兼容
   Story Oracle 的 `AUTO` 诊断；若无法验证关闭，它会阻断变量写入而不是冒险双写。
7. 查看“环境自检 → TavernDB 可选协作”。“非托管”表示医生没有观察到合作 API，
   外部数据库写入一致性未知；这不是错误，也不影响医生自己的安全修复。

这次操作只更新 `mvu-auto-doctor` 自身。数据库、骰子前端、缝合怪、推进重roll
助手和其他脚本都保持现状；不要点击这些脚本行的更新、分支、删除或重装按钮。

### 全新在线安装

尚未安装自动医生时，打开“扩展 → 安装扩展”，填写：

```text
仓库地址：https://github.com/magilittle0-byte/mvu-auto-doctor
分支或标签：留空（默认 main）
```

选择“仅为当前用户安装”（单用户环境直接选择“安装”），完成后刷新页面。

## 2. 和现有脚本一起使用

数据库本体、可视化前端、骰子前端、缝合怪、推进重roll助手V2，以及其他
已经调好的脚本都可以继续启用，不需要为了测试自动医生而删除或重装。

- 自动医生只使用自己的 `mvuad-*` 控件和扩展存储，不会覆盖推进重roll助手的
  `reroll-helper-btn`、`Reroll_Global_Settings` 或 `Reroll_Cache_*`。
- 第三方数据库可以完全不调用医生 API。若它主动注册可选协作，医生可以向它公开
  settled/failed/stale 终态；未注册时医生既不接管也不阻断，只把外部一致性标为
  “未知/非托管”。这不改变医生自身 failed/stale/late 结果零写入的不变量。
- 不要导入或启用旧“数据库最终正文桥”。当前发行包里的同名条目只是默认停用的
  升级占位；它不会调用 `triggerUpdate()`。数据库每轮只读取接受的 `<content>`
  独立填表，不等待变量医生，也不在变量写入后重复填表。
- 不要重新启用旧 `spv8.4` 源码改写脚本。作者数据库升级时只改作者原版入口的
  版本标签；独立桥无需跟着改数据库版本，也不要把两段脚本重新拼成一个加载器。
- 推进重roll助手触发重生成后，自动医生会重新核对聊天、消息、swipe、
  generation、branch 和正文指纹。旧回复上的医生任务会变成 `stale`，不能污染
  新分支。
- 骰子结果、装备槽位、资源消耗和 MVU Schema 仍以角色卡与启用世界书的明确
  合同为准；自动医生不会猜字段，也不会改写骰子前端或缝合怪文件。

正常使用时可以直接点推进重roll助手。若悬浮工具页正显示任务执行中，建议先
观察该任务完成或取消后再重roll；即使直接重roll，旧任务也必须被稳定屏障拦住，
不会落到新回复或数据库里。

### 数据库的安装顺序

1. 停用旧的数据库源码改写兼容层，保留作者原版数据库本体。
2. 将作者原版数据库入口按作者公告指向当前版本，并确认它单独可以加载。
3. 不安装任何会在医生结算后调用数据库通用更新入口的桥接脚本。
4. 刷新酒馆，确认作者数据库本体能独立处理每轮 `<content>`。医生环境自检中
   未注册的第三方数据库显示“未知/非托管”是信息状态，不代表错误。

## 3. 日常使用

1. 正常发送消息并等待 AI 回复完成。自动医生会等待普通 MVU 更新稳定，再进行
   本地合同检查和必要的模型诊断。
2. 点击悬浮球“脉”可打开工具页；世界、事件、论坛和工具页使用同一聊天上下文。
3. 变量存在可证明问题时，补丁先在内存中校验，再精确写入当前楼层并回读。
   正文只读；硬合同问题只报告，不会创建、覆盖或切换正文 swipe。
4. 危险的导演动作必须显示目标和影响范围并由你确认。没有宿主桥或证据不足时，
   页面会显示 `unresolved`，不会直接写状态。
5. 内置论坛默认手动刷新。它读取本轮接受的正文并独立运行，不阻塞变量医生或
   数据库；重载页面后主题和评论应继续存在。
6. 工具页的操作时间线可以查看任务阶段、取消与失败原因；默认诊断只记录 verdict、
   数量、错误码、哈希和必要耗时，User-Agent 只保留平台与内核大版本，不包含剧情
   摘要、finding/decision理由、私人聊天、完整提示词、密钥或原始请求载荷。
7. 人物二审费用按独立月度收据累计；审计列表即使只保留最近30条，¥5软提醒和¥10
   硬停止也不会回退。升级当月若旧窗口无法证明完整费用，会安全停止自动二审至
   下个自然月。HTTP失败、超时或结构无效时零关系写入，不会统一 revert 或删除路径。

### NPC 分片与自由提示词

- “Actor Shard / NPC分片”默认关闭。开启后每个稳定回复最多额外调用1—5次轻量
  模型，默认上限2；单个worker失败或超时会自动降级到原活世界流程。
- worker只生成后台候选，不直接写MVU、世界书、论坛或聊天正文；重抽、切swipe或
  切聊天会让整批候选作废。
- “世界连续性自定义指令”和“NPC分片自定义指令”最多各6000字符。系统不做题材/
  NSFW语义过滤，也不提供默认破限示例；它们只影响叙事与模拟候选。
- 自定义指令不能代替玩家授权、事实证据、事务、危险确认、分支或硬字段校验。
  脱敏诊断只显示是否启用、长度和哈希，不导出全文。
- 控制成本时保持关闭即可；需要更细的幕后角色活动时先用1—2个worker试跑。

## 4. 推荐的 RC 共存测试

请用一份已备份的现有长聊天完成下面这组测试：

1. 打开1.x时期的聊天，确认旧消息、数据库状态和角色卡仍可读。
   当前候选已验证65消息、48个swipe、约3.5 MiB的合成旧档回归；默认迁移窗口为有界
   8 MiB，超过上限会保守回退为1.x只读视图。
2. 正常生成一轮，等自动变量任务显示终态，确认变量补丁绑定当前目标且正文 swipe
   未被医生改写；连续性、论坛和数据库各自读取同一轮接受正文，不互设长时间屏障。
   第三方数据库未提供合作能力时只验证共存，不声称其受医生事务控制。
3. 再生成一轮，并在医生仍在处理时使用推进重roll助手V2；确认旧任务变为
   `stale` 或被取消，新回复没有混入旧分支的正文、变量、论坛或数据库写入。
4. 检查骰子前端的结果、资源消耗和装备状态没有被重复结算。
5. 检查缝合怪、数据库可视化及其他现有脚本仍能打开，控制台没有新增错误。
6. 在手机宽度打开悬浮页和论坛，展开、收起帖子，确认没有横向溢出。
7. 刷新页面，确认聊天、数据库状态、论坛主题和评论仍然存在；医生对相同
   message/swipe/generation/branch/content fingerprint 不重复执行，旧分支终态历史
   和世界注入收据仍可审计。
8. 若启用NPC分片，分别用1、3、5个worker验证额外调用上限；在worker运行中重抽
   或切聊天，确认旧批次stale，且没有旧分支连续性、论坛或数据库写入。

若真实结果与上述门禁不一致，请停止继续覆盖测试并按下一节提供脱敏信息；真实
环境结果优先于模拟测试，候选不能因此被放行。

## 5. 回滚到旧版

1. 取消或等待自动医生当前任务结束，并用酒馆界面保存当前聊天备份。
2. 停止继续生成，保留现场并反馈“需要回滚 RC”。维护者已将更新前 `main`
   固定备份为 `codex/backup-main-pre-v2.0.0-rc.1-20260727`，会从该快照向
   `main` 发布一个可正常向前更新的回滚提交。
3. 收到回滚已发布的确认后，只点击自动医生这一行的“更新”并刷新。不要删除原聊天、
   数据库、其他脚本，也不要手工删除迁移审计数据。
4. 若更新失败、摘要校验不一致或旧聊天不可读，停止写入并保留现场，按
   [`MIGRATION_ROLLBACK_GUIDE.md`](MIGRATION_ROLLBACK_GUIDE.md) 的保守恢复步骤处理。

## 6. 反馈问题时提供什么

可以提供：自动医生版本、SillyTavern/TauriTavern版本与提交号、手机或桌面尺寸、
是否使用重roll、可复现步骤、任务状态、控制台错误名称和脱敏诊断包。

不要提供：API 密钥、私人聊天原文、完整提示词、原始模型 payload、包含凭据的
请求头，或整个用户数据目录。
