# MVU Auto Doctor 2.0 阶段交接模板

> 每阶段结束时复制本模板为 `docs/2.0/handoffs/PHASE_<N>_HANDOFF.md` 并填写。不得把“见聊天记录”作为任何字段的答案。

## 1. 身份

- 阶段：
- 交付日期：
- 仓库：
- 分支：
- PR：
- 基础提交 SHA：
- 最终提交 SHA：
- 工作区是否仍有未提交修改：
- 未提交修改是否属于用户且已保留：

## 2. 本阶段范围

- 授权目标：
- 明确非目标：
- 实际完成：
- 有意未做：

## 3. 权威文件

列出本阶段读取并作为依据的准确仓库路径与版本/提交：

- 产品规格：
- 数据/事务协议：
- 回放矩阵：
- fixture schema：
- fixture corpus：
- 上一阶段交接：
- 其他：

## 4. 产物与接口

每项写明路径、用途、公开入口和主要不变量：

| 产物 | 路径 | 对外 API / 命令 | 不变量 |
| --- | --- | --- | --- |
|  |  |  |  |

## 5. 数据与迁移

- 新增/改变的数据模型：
- schemaVersion：
- 1.x读取策略：
- 写回策略：
- 未知字段保留证明：
- 无法迁移的数据与可见降级：
- 回滚方式：

## 6. 已决决策与未决决策

| ID | 已决/未决 | 决策或问题 | 证据 | 影响 | 下一负责人 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

不得省略仍会改变下一阶段实现方向的未决决策；即使没有，也要明确填写“无”及其核查范围。

## 7. 测试与验收

列出实际运行的完整命令、退出码和结论：

```text
命令：
退出码：
结论：
```

- 新增结构测试：
- 新增行为测试：
- 既有相关回归：
- 浏览器测试：
- 真实 SillyTavern QC：
- fixture 覆盖：
- 未激活的 todo/pending 行为及激活条件：

## 8. 隐私与安全检查

- 增量密钥扫描命令与结果：
- 私人正文扫描命令与结果：
- fixture 最大文本长度：
- 是否包含绝对用户目录：
- 是否读取但未修改私有归档：
- 其他敏感数据处理：

## 9. 差异审计

- `git status --short --branch`：
- `git diff --stat <base>...HEAD`：
- 预期文件：
- 无关文件：
- 无关文件如何被保留/排除：

## 10. 已知风险

| 风险 | 触发条件 | 影响 | 当前缓解 | 下一阶段动作 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 11. 运行与故障恢复

- 可观察状态：
- 软取消：
- 硬超时/看门狗：
- 迟到结果处理：
- 写前恢复记录：
- 写后回读：
- 手动恢复步骤：

不适用时必须说明为什么不适用。

## 12. 下一阶段准确入口

必须是一段可直接复制给下一任务的指令，至少包含：

1. 从哪个提交 SHA 开始；
2. 首先完整读取哪些文件；
3. 允许修改哪些目录；
4. 第一个应实现的 API/用例；
5. 必须保持的旧行为；
6. 完成门与测试命令；
7. 禁止事项。

```text
下一阶段入口：
```

## 13. 发布状态

- 本地提交：
- 远端分支：
- PR状态（draft/ready）：
- 基础分支：
- 是否合并 main：否，除非维护者审阅后明确执行。
- 外部阻塞：
