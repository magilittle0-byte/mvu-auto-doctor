# MVU Auto Doctor 2.0 RC 回归矩阵

日期：2026-07-29

结论：修复完成，最终发布证据为 `pass`

本矩阵只使用用户提供记录的脱敏统计、结构计数、错误码与目标身份类别。私人正文、
完整提示词、密钥、角色卡文本和 canary 均未复制到仓库。
“未复制到仓库”不禁止授权的正常模型推理：作者公开卡及其正常入模上下文可在独立
QC 聊天发送给用户明确选择的模型；报告仍只保留脱敏状态、计数、摘要和耗时。

| ID | 严重度 | 触发与目标身份 | 预期 / 实际 | 写入影响 | 根因 | 修复与验证 |
| --- | --- | --- | --- | --- | --- | --- |
| RC-DB-01 | critical | 真实 spv8.4 与 legacy spv5.5.6 均未注册医生协议 | 应正常共存 / 旧实现会把无协议数据库标红 | 医生不可用；也不能证明第三方强一致 | 把自定义合作协议误做成第三方强制前置条件 | 改为可选协作；两版均完成结构发现式 CRUD/refresh/callback/UI/切聊/重载/清理，医生保持 `unmanaged/unknown` info |
| RC-CONTENT-02 | critical | 最新 assistant、当前 swipe，存在1个 `<content>` 开始且0个闭合 | 应恢复唯一可证结构 / 硬审计报 `content-tag-count` | 连续性与论坛被阻断，正文没有修正版 | 缺少确定性的尾标签本地恢复 | 仅在唯一无歧义时于 options/UpdateVariable/状态占位符前闭合，并创建新 swipe；原文不覆盖 |
| RC-UPDATE-03 | high | 逻辑消息索引4、修复后当前 swipe | 应只有1个最终 UpdateVariable / 实际有2个完整块 | 重载或下游解析可能重复应用/误判 | 修复追加只替换首块，旧完整块残留 | 删除所有完整旧块后插入1个规范块；保留其他正文机制 |
| RC-MODEL-04 | critical | 同一完整目标并发5次 `MESSAGE_RECEIVED` | 每个任务至多调用一次 / 旧实现可重复建 barrier 和调用模型 | 模型风暴、重复修复或错写 | 自动事件缺少“已存在目标结算”复用 | 创建 barrier 前按完整身份复用 pending/terminal 记录；真实事件风暴只产生1条 settled 记录、每任务最多1次调用、错写0 |
| RC-SWIPE-05 | high | regenerate、新 swipe、切聊天或迟到回调 | 只写精确当前身份 / 元数据数组可能与 swipes 长度错位 | 后续身份选择或回滚可能指向错误 swipe | 追加 swipe 前未归一 `swipe_info` | 先补齐/裁齐元数据再追加；failed/stale/late 零写入回归继续覆盖 |
| RC-CARD-06 | high | 5.5.3 PNG v3、陌生英文 Schema、随机表名/列名与未知前端对象 | 当前卡只能是样本 / 旧测试把单一卡结构写成产品范围 | 其他模板可能漏检或被错误套用当前方言 | 测试范围与核心兼容边界混淆 | 核心只依赖动态 JSON Pointer、实际 MVU 数据和宿主生命周期；未知 Schema 只读审计零重塑，医生对外部表格/API调用为0；5.5.3仅保留为 fixture |
| RC-MOBILE-07 | high | SillyTavern 宿主 `.menu_button{width:min-content;display:flex}`，390×844 | 工具按钮可点且隐藏控件不可见 / 按钮曾缩成竖排且 cancel 被显示 | 手机端几乎不可操作 | 宿主 CSS 覆盖扩展宽度与原生 `[hidden]` | 医生作用域恢复 `[hidden]`，按钮 `width:100%;min-width:0`，可见操作与关闭按钮至少42px |
| RC-PERF-08 | medium | 大脚本树、长档案、队列扫描 | 应线性处理 / BFS 使用 `shift()` 反复搬移数组 | 长局中放大主线程开销 | 队列实现为潜在 O(n²) | 改为索引游标遍历；模型请求仍受去重和超时控制 |
| RC-QC-09 | critical | 旧结构化报告与本次真实记录比较 | 真实证据优先 / 旧报告曾错误声称控制台0错误和完整真实门通过 | 可能错误发布不可用版本 | QC 把历史局部成功当成当前结论 | 先重建 blocked 收据，补齐同主模型A/B、真实Scenario与第三方共存后才改为pass；校验器按责任方分别要求医生/数据库/伴生前端0错误 |
| RC-ENV-10 | environment | 后台真实 SillyTavern 1.18.0 独立 QC 聊天 | 应具备 MVU API和成功模型 / 早期沙箱浏览器误报MVU缺失及网络失败 | 旧报告错误阻断现场判断 | 沙箱网络与合成卡漏保留宿主 `chat` 标识制造了假阴性 | 非沙箱无头复验确认MVU完整、自检ok；严格修复和下游模型均HTTP 200。第三方JS-Slash错误单列，不归因医生 |
| RC-FORUM-11 | high | 用户记录中论坛被上游硬合同阻断 | 修复后应独立恢复 / 原记录无论坛调用 | 上游修复后若不恢复，论坛仍不可用 | 正文硬合同失败截断下游 | 当前源码固定QC聊天真实复验：连续性与论坛均applied，2/2模型成功，4帖12评论 |
| RC-ACTOR-12 | high | 严格 Actor 示例使用占位值，但解析器要求值来自候选事实白名单 | 1/3/5 worker 均应完成 / 模型成功响应仍被本地拒绝 | Actor 全部降级失败 | 示例本身与严格候选白名单冲突 | 示例直接使用候选时间、地点、知识、动作与来源；定点测试通过，真实1/3/5分别1/1、3/3、5/5成功，失败0 |
| RC-LIFECYCLE-13 | critical | 医生确定性修复创建新 swipe，同时已有连续性/论坛 checkpoint | 应把修正版作为同一流程继任目标 / 内部 `MESSAGE_SWIPED` 曾被当成用户切换，barrier仍绑定旧分支 | 自己把自己标成stale，下游不结算 | 内部事件与外部用户事件共用监听器，终态没有显式最终目标 | 内部 swipe 事件不回滚 checkpoint；工作流显式传播 `finalTarget`，phase-6终态绑定新swipe完整身份；宿主浏览器回归通过 |

## 通用兼容边界

- 医生核心不识别或改写 TavernDB 的表名、列名、行号与 DOM；数据库公开 API 只由
  隔离 QC 探针调用，生产医生调用数为0。
- MVU 修复以当前 `stat_data`、当前卡实际 Schema/规则、动态 JSON Pointer 和完整
  `chat/message/swipe/generation/branch/fingerprint` 身份为准，不内置当前卡路径。
- 陌生英文 `nebulaLedger` Schema 的24轮真实长局完成24/24 settled、24个不同目标
  身份和24/24真实模型成功；49条消息与24条结算记录重载后恢复。
- 装备、骰子和缝合怪识别属于保守 legacy 方言适配：只有实际文本/结构出现对应合同
  信号时才启用；未知前端继续使用通用变量修复与生命周期门，不猜槽位、资源或标签。
- 表格模板、数据库内缝合怪剧情预设、缝合怪专属显示正则、骰子前端和推进重roll
  已同时启用完成现场共存；这证明黑盒兼容与命名空间隔离，不把它扩大解释为医生
  接管或专项审计所有第三方内部语义。

## 5.5.3 卡结构兼容样本

- 原件是 PNG v3 角色卡；只读解析得到77个世界书条目、3个 TavernHelper 脚本、
  8个 regex 脚本，未发现需要医生强制接管的 TavernDB 合同。
- 旧真实卡结构计数为75个世界书条目、2个 TavernHelper 脚本。差异说明作者在卡内
  增加了内容/脚本，不等于 schema 协议改变。
- 医生现在兼容 `tavern_helper.scripts`、`tavern_helper.script.scripts`、
  `TavernHelper` 大小写变体、扁平 legacy 容器和嵌套脚本组。
- `character_version` 为空，因此兼容判断不能依赖该字段；应以 v3 数据结构、活动
  世界书与实际脚本内容字段为准。
- 卡侧建议仅为：作者可填写可读版本号，并避免同一 MVU 规则重复启用。医生不要求
  卡作者加入 barrier API，本任务也没有修改原卡。

## 当前真实环境结果

- 桌面1280×720：面板在视口内，无横向溢出；4个工具按钮为140×42，关闭按钮42×42。
- 手机390×844：面板边界为左6、右384、上约172、下780；4个工具按钮为332×42，
  可见最小控件高度42，无横向溢出。
- 隔离 settings 真实安装当前安全入口 spv8.4；`AutoCardUpdaterAPI` 有114个方法。
  结构发现式最小写入/回读/更新/刷新/回调、设置 UI、切聊、保存、重载和清理均通过，
  导出哈希在切聊与重载后不变。
- legacy spv5.5.6 以兼容样本单独复验，88个 API 方法和同一黑盒流程通过；它不作为
  生产推荐。完成后已恢复 spv8.4，未让两个版本并发。
- 两版均无医生合作协议，因此医生准确显示 `unmanaged`、一致性 `unknown`、自检
  `info`；没有要求数据库、TavernHelper 或卡作者脚本适配医生。
- 总体医生环境为ok：MVU读取、解析、精确写回接口完整；严格/轻量模型通道均配置。
- 严格变量真实闭环1次成功：缺失闭标签形成新swipe、重复更新块归一、计数2→0；
  原swipe保留，保护片段命中0。
- 当前源码连续性与论坛真实模型2/2成功，均applied，生成4帖12评论。
- regenerate、swipe、chat-switch三类真实飞行中请求均到达HTTP 200并返回stale；
  原正文保持不变，旧目标MVU计数保持2。
- Actor Shard真实1/3/5 worker均全部成功；事件风暴、内部修正版 swipe 和三类 stale
  与 TavernDB 同时加载时未发生重复或旧目标写入。
- 24轮黑盒长局完成24/24真实模型调用，数据库刷新前后 SHA-256 一致；重载后结构
  SHA-256 与24条 settled 收据一致。临时 Schema 移除后的 MVU 值不宣称持久恢复。
- 长局控制台100条重复资源错误全部归类为宿主/JS-Slash；医生和 TavernDB 错误均0。
  医生未修改第三方，不能宣称全宿主0错误。
- 同一酒馆主模型 `gpt-5.4-mini` 完成合同关/开2组配对A/B：4次请求均HTTP 200，
  合同只在B组最终提示中出现，未选择选项从模型历史移除但存档聊天、系统格式和用户
  实际选择保持不变；不受支持的动机归因由A组1次降为B组0次。
- 真实 Scenario Plan 从不可覆盖v0形成1次 `player_action` 来源修订：4个字段保留
  精确前后值，5项既有成果保留，baseline/amendment来源引用与UI追踪均可见。
- 当前 spv8.4 中同时挂载表格模板、骰子前端、推进重roll与缝合怪剧情预设；缝合怪
  2个任务在API与存储重载后保持，数据库UI调用其公开刷新后显示2项。49条聊天哈希
  未变、模型调用0、医生/数据库/伴生前端错误0、重roll无重复挂载。
- 浮层背景改为实色；桌面与手机截图在通知消失后无宿主正文透出。
